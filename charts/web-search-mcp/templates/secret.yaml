apiVersion: v1
kind: Secret
metadata:
  name: {{ include "web-search-mcp.fullname" . }}-secrets
type: Opaque
stringData:
{{- range $k, $v := .Values.secrets }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
